FROM deeplabcut/deeplabcut:latest-gui-jupyter

COPY ["./src", "./examples", "./docker/requirements.inference", "./"]

RUN pip install -r requirements.training

ENV PYTHONPATH "${PYTHONPATH}:./src"

ENTRYPOINT ["/bin/bash"]